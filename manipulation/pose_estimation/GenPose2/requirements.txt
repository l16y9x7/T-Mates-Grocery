# GenPose2 统一依赖（PyTorch 请按 CUDA 版本单独安装，勿让 pip 擅自升级）
# 安装：pip install -r requirements.txt --upgrade-strategy only-if-needed
#
# 参考环境：torch==2.1.0+cu121, numpy==1.26.3, opencv-python==4.9.0.80

# ---- 核心 ----
opencv-python==4.9.0.80
scipy==1.12.0
numpy==1.26.3
tensorboardX==2.6.2.2
tensorboard==2.17.0
open3d==0.18.0
pyrealsense2==2.55.1.6486
ipdb
matplotlib
tqdm
scikit-learn
requests>=2.28.0
pillow>=9.4.0

# ---- HTTP 服务（http_server.py）----
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
python-multipart>=0.0.9
pycocotools>=2.0.6

# ---- Gradio UI（run_ui.py / start.sh）----
gradio>=4.44.0,<7
trimesh>=4.0.0

# ---- YOLO 分割（segment/yolo_seg_backend.py）----
# 若 pip 试图升级 torch，请加 --upgrade-strategy only-if-needed
ultralytics>=8.2.0,<8.5.0

# ---- 相机 / 实时推理（runners/infer_camera.py）----
OpenEXR==3.3.5
holodex==0.11.11
termcolor==3.1.0
imageio[ffmpeg]==2.37.0
hydra-core>=1.3.2
iopath>=0.1.10
